{
    "id": "Test::IncompatibleRequestPropertiesRule",
    "name": "Test Rule",
    "description": "This is a test detection rule with invalid properties for a given request type.",
    "version": "0.1",
    "steps": [
        {
            "id": "Test step 1",
            "description": "This is a no-op step.",
            "requestType": "api",
            "request": {
                "command": "echo",
                "args": [
                    "No operation performed."
                ]
            }
        }
    ]
}
